name: create PR by github-actions[bot]
on:
  pull_request:
    branches:
      - 'main'
    types:
      - closed
env:
  REVIEWER: YutaCurry
jobs:
  tf_apply:
    uses: ./.github/workflows/_tf_apply.yml
    with:
      WORK_DIR: bin/main
      ENV: prod
    secrets: inherit